[package]
name = "oneterm-local-shell"
description = "Local shell via PTY for OneTerm (alacritty_terminal::tty)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish.workspace = true

[features]
terminal-diagnostics = []

[lints]
workspace = true

[dependencies]
oneterm-core.workspace = true
oneterm-terminal.workspace = true
alacritty_terminal.workspace = true
async-channel.workspace = true
polling.workspace = true
log.workspace = true

[dev-dependencies]
oneterm-terminal = { workspace = true, features = ["test-support"] }
