[package]
name = "oneterm-pty"
description = "Pseudo-console transport for OneTerm (ConPTY on Windows, openpty on Unix)."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish.workspace = true

[lints]
workspace = true

[dependencies]
# `polling` is a PUBLIC dependency: Poller/Event/PollMode appear in the
# `EventedReadWrite` signatures, so a `polling` bump breaks this crate's API.
polling.workspace = true
log.workspace = true

[target.'cfg(windows)'.dependencies]
windows-sys.workspace = true

[target.'cfg(unix)'.dependencies]
libc.workspace = true
