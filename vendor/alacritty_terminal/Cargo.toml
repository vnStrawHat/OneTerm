[package]
edition = "2024"
rust-version = "1.85.0"
name = "alacritty_terminal"
version = "0.26.0"
authors = [
    "Christian Duerr <contact@christianduerr.com>",
    "Joe Wilm <joe@jwilm.com>",
]
description = "Library for writing terminal emulators (myTerm2 fork — CREATE_NEW_PROCESS_GROUP)"
homepage = "https://alacritty.org"
readme = "README.md"
license = "Apache-2.0"
repository = "https://github.com/alacritty/alacritty"
resolver = "2"

[features]
default = ["serde"]
serde = [
    "dep:serde",
    "bitflags/serde",
    "vte/serde",
]

[lib]
name = "alacritty_terminal"
path = "src/lib.rs"

[dependencies]
base64 = "0.22.0"
bitflags = "2.4.1"
home = "0.5.5"
libc = "0.2"
log = "0.4"
parking_lot = "0.12.0"
polling = "3.8.0"
regex-automata = "0.4.3"
unicode-width = "0.2.0"
vte = { version = "0.15.0", default-features = false, features = ["std", "ansi"] }
serde = { version = "1", features = ["derive", "rc"], optional = true }

[target.'cfg(unix)'.dependencies]
rustix-openpty = "0.2.0"
rustix = { version = "1.0.0", default-features = false, features = ["std"] }
signal-hook = "0.4.3"

[target.'cfg(windows)'.dependencies]
piper = "0.2.1"
miow = "0.6.0"
windows-sys = { version = "0.59.0", features = [
    "Win32_System_Console",
    "Win32_Foundation",
    "Win32_Security",
    "Win32_System_LibraryLoader",
    "Win32_System_Threading",
    "Win32_System_WindowsProgramming",
]}