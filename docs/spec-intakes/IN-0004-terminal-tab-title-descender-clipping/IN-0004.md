# Spec Intake: Terminal tab title descender clipping

ID: IN-0004
Date: 2026-08-11
Type: maintenance
Lane: tiny

## Source

User-reported rendering defect with screenshot evidence: lowercase descenders such as `p`, `g`, `q`, and `y` were clipped in terminal tab titles.

## Requested Outcome

Prevent terminal tab-title glyph descenders from being clipped by the nested GPUI label content mask.

## Project Impact

- Product surface: terminal tab-strip title rendering.
- Existing title resolution, truncation, rename, drag, and close behavior must remain unchanged.
- The correction is local to GPUI label styling and does not change persistence or public APIs.

## Candidate Product Contracts

| Contract | Purpose | Source or owner |
| --- | --- | --- |
| `docs/terminal-split/03-drag-drop.md` | Custom terminal tab-title element and drag source | `oneterm-terminal-view` |
| `docs/terminal-split/06-integration.md` | `TerminalPanel::title()` integration boundary | `oneterm-terminal-view` |

## Candidate Work Packets

- [x] `BUG-006` — remove the nested title label's clipping mask while retaining width-constrained ellipsis.

## Architecture and Boundary Questions

- Runtime and owning boundary: `oneterm-terminal-view` owns the custom title label; gpui-component owns the surrounding tab container.
- Data ownership and lifecycle: no data or persistence changes.
- Auth, security, privacy, or audit: none.
- External systems and side effects: none.
- Public interfaces and compatibility: no public API changes.

## Validation Shape

| Layer | Expected proof |
| --- | --- |
| Focused | Style regression test proving the nested label creates no overflow content mask |
| Unit | `cargo test -p oneterm-terminal-view` |
| Integration | `cargo test --workspace` |
| E2E | Manual desktop confirmation because no screenshot baseline exists |
| Platform / Release | Format, clippy, and workspace build gates |

## Open Decisions and Questions

- [x] No blocking decisions remain.

## First Action or Handoff

- [x] `BUG-006` implemented and verified the no-content-mask styling invariant; final glyph-paint confirmation remains a manual desktop check.

## Harness Delta

- [x] No reusable Harness change was required.

> Keep only useful detail. Do not fabricate project facts, create empty downstream artifacts, or turn unresolved product decisions into implementation assumptions.
