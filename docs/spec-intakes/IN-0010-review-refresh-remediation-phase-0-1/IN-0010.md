# Spec Intake: Remediate Phase 0 and Phase 1 of the 2026-08 refresh review

ID: IN-0010
Date: 2026-08-17
Type: maintenance
Lane: high-risk (auth, data loss, public contracts are touched)

## Source

`docs/review-refresh-2026-08/09-remediation-plan.md` — Phase 0 ("stop the bleeding") and Phase 1
("contracts and CI identity") of the full-project refresh review.

## Requested Outcome

Every Phase 0 and Phase 1 item in the remediation plan is implemented with the regression tests the
review asked for, the workspace quality gates stay green, and the review checklists are ticked.

## Project Impact

Touches the terminal engine, both backends, terminal-view, settings, workspace/app wiring, the SFTP
contract (`SftpBackend`), the updater, Cargo manifests, vendor tooling and CI. Product behaviour is
preserved except where the review documents a defect; public traits (`SftpBackend`, `TerminalSession`)
change shape as recorded in the review.

## Candidate Product Contracts

| Contract | Purpose | Source or owner |
| --- | --- | --- |
| `docs/terminal-backend.md` | Backend/UI event delivery and locking | Backends |
| `docs/ssh-client-connect.md`, `docs/ssh-authentication.md` | Host key and auth behaviour | `oneterm-ssh` |
| `docs/sftp-browser-design.md` | SFTP contract and browser behaviour | `oneterm-sftp-ui`, `oneterm-core` |
| `docs/auto-update.md` | Installer behaviour | `oneterm-update` |
| `docs/agents/persistence.md` | Persistence mechanics | shared |
| `docs/agents/dependencies.md`, `docs/agents/crate-dependency-rules.md` | Manifests, rev lock, vendoring | workspace |
| `docs/agents/error-policy.md`, `docs/agents/code-style.md` | Runtime error and code rules | project |

## Candidate Work Packets

- [x] `WK-0001` — Phase 0 (stop the bleeding).
- [x] `WK-0002` — Phase 1 (contracts and CI identity).

## Architecture and Boundary Questions

- Runtime and owning boundary: unchanged crate layering; shared pump logic may move down into `oneterm-terminal`.
- Data ownership and lifecycle: docks.json/sessions.json/update_config.json writers become single-writer.
- Auth, security, privacy, or audit: RSA hash selection, host-key algorithm change handling, ConPTY quoting, keyboard-interactive.
- External systems and side effects: updater installer file moves; CI matrix.
- Public interfaces and compatibility: `SftpBackend` (`RemotePath`, `TransferHandle`, `remove_dir_all`), `TerminalSession::take_events`.

## Validation Shape

| Layer | Expected proof |
| --- | --- |
| Focused | Regression tests listed per item in `docs/review-refresh-2026-08/06-testing.md` (TEST-06..TEST-12, TEST-22, TEST-23). |
| Unit | `cargo test -p <crate>` for each touched crate. |
| Integration | `cargo test --workspace`. |
| Platform / Release | `cargo fmt --all -- --check`; `cargo clippy --workspace --all-targets -- -D warnings`; `cargo build --workspace`; the four Python checks. |

## Open Decisions and Questions

- [x] Work lands on `fix/review-remediation-phase-0-1`, not `main`; the user decides when to merge.

## First Action or Handoff

- [x] Parallel agents per disjoint file set, one wave per phase, integration merge and full gate after each wave.

## Harness Delta

- [x] No reusable Harness change identified.
