# Spec Intake: Selected Space highlight content overlap

ID: IN-0005
Date: 2026-08-12
Type: maintenance
Lane: tiny

## Source

User report: after splitting and selecting a Space, the highlight border paints over terminal text and should be improved.

## Requested Outcome

Keep the selected Space highlight visually prominent without painting its border over terminal text or placeholder content.

## Project Impact

This is a localized rendering correction for split terminal Spaces. It does not change split-tree state, focus behavior, terminal data, or public APIs.

## Candidate Product Contracts

| Contract | Purpose | Source or owner |
| --- | --- | --- |
| `docs/terminal-split/05-rendering-theme.md` | Defines Space borders and the active-Space highlight. | Terminal Split design |

## Candidate Work Packets

- [x] `BUG-0007` — render the selected state on the existing Space frame rather than as a content overlay.

## Architecture and Boundary Questions

- Runtime and owning boundary: `terminal-view::space::render` owns Space chrome.
- Data ownership and lifecycle: unchanged; `SpaceTree.active` remains the source of truth.
- Auth, security, privacy, or audit: none.
- External systems and side effects: none.
- Public interfaces and compatibility: none.

## Validation Shape

| Layer | Expected proof |
| --- | --- |
| Focused | Regression test verifies active and inactive frame colors. |
| Unit | `oneterm-terminal-view` tests pass. |
| Integration | Workspace tests where practical. |
| E2E | Not available for pixel rendering. |
| Platform / Release | Mandatory format, clippy, and workspace build gates. |

## Open Decisions and Questions

- [x] Use the existing outer Space frame as the highlight; no user decision is required because this matches the accepted Terminal Split contract.

## First Action or Handoff

- [x] Review the renderer and owning design, then make the smallest rendering-only correction.

## Harness Delta

- [x] None.

> Keep only useful detail. Do not fabricate project facts, create empty downstream artifacts, or turn unresolved product decisions into implementation assumptions.
